<% cache do %>
    <h1>Fotoalbum</h1>

    <div style="margin-bottom: 10px;">
        Suche Dir unten ein Fotoalbum aus und klicke auf das Bild um die darin enthaltenen Fotos anzuschauen.
    </div>

    <% @photosets.each do |set| %>
        <% style_class = cycle("photoset_left", "photoset_right") %>
        <%= render :partial => 'photoset', :locals => { :photoset => set, :style_class => style_class } %>
    <% end %>

    <div class="clear"></div>

<% end  %>

<% if permitted_to? :manage, :photos %>
	<% content_for :sub_menu do %>
		<li class="side_bar"><%= link_to "Fotos hinzufügen (Flickr)", "http://www.flickr.com/", :target => "_blank" %></li>
	<% end %>
<% end %>