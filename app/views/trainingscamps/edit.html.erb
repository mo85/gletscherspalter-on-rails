<h1>Trainingslager Anpassen</h1>

<% form_for @event, :html => { :class => :default } do |f| %>
	
	<div>
		<%= f.label "Datum" %>
		<%= f.datetime_select :date %>
	</div>
	
	<div>
		<%= f.label "Ort" %>
		<%= f.text_field :locality %>
	</div>
	
	<div>
		<%= f.label "Notiz" %>
		<%= f.text_area :note %>
	</div>
	
	<div>
		<%= f.submit "Anpassen" %>
	</div>
<% end %>

<% content_for :sub_menu do %>
	<li class="side_bar"><%= link_to "Zurück", events_path %></li>
<% end %>