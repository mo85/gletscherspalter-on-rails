<h1>Spielplan Saison <%=h current_season.to_s %></h1>
	
<div style="margin-top: 10px;">
	<%= render :partial => "game_table", :locals => { :games => @games, 
		:show_actions => true, :show_result => true, :check_box_needed => false 
		} %>
	<div style="margin-top: 5px;">
	<hr/>
		<%= will_paginate @games %>
	<hr/>
	</div>
</div>

<% content_for :sub_menu do %>
	<% if permitted_to? :manage, :games %>
	<li class="side_bar"><%= link_to "Neues Spiel erstellen", new_game_path %></li>
	<% end %>
	<li class="side_bar"><%= link_to "Spielplan als PDF", games_path(:format => "pdf") %></li>
<% end %>