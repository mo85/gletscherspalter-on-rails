<h1><%= @player.user.full_name %> editieren</h1>

<% form_for [@player], :html => {:class => :default} do |f| %>
  <%= f.error_messages %>

  <div>
	<%= f.label "Nummer" %>
	<%= f.text_field :number %>
  </div>

  <div>
	<%= f.label :position %>
	<%= f.select :position, PLAYER_POSITIONS, :promt => "Select a position" %>
  </div>

  <div>
    <%= f.submit "Anpassen" %>
  </div>
<% end %>

<%= link_to 'Profil Seite', @player %> |
<%= link_to 'Team Seite', players_path %>
